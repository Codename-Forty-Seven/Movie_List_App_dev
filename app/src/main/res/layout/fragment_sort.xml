<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:padding="10dp">

    <TextView
        android:id="@+id/tv_sort_by_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:background="@drawable/design_for_menu"
        android:clickable="true"
        android:focusable="true"
        android:fontFamily="@font/roboto"
        android:gravity="center"
        android:padding="10dp"
        android:text="@string/txt_title"
        android:textColor="@color/grey_txt"
        android:textSize="20sp"
        app:layout_constraintBottom_toTopOf="@+id/tv_sort_by_release_date"
        app:layout_constraintEnd_toEndOf="@+id/tv_sort_by_release_date"
        app:layout_constraintStart_toStartOf="@+id/tv_sort_by_release_date" />

    <TextView
        android:id="@+id/tv_sort_by_release_date"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:background="@drawable/design_for_menu"
        android:clickable="true"
        android:focusable="true"
        android:fontFamily="@font/roboto"
        android:gravity="center"
        android:padding="10dp"
        android:text="@string/txt_released_date_2"
        android:textColor="@color/grey_txt"
        android:textSize="20sp"
        app:layout_constraintBottom_toTopOf="@+id/tv_cancel_sort"
        app:layout_constraintEnd_toEndOf="@+id/tv_cancel_sort"
        app:layout_constraintStart_toStartOf="@+id/tv_cancel_sort" />

    <TextView
        android:id="@+id/tv_cancel_sort"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="8dp"
        android:background="@drawable/design_for_menu"
        android:clickable="true"
        android:focusable="true"
        android:fontFamily="@font/roboto"
        android:gravity="center"
        android:padding="10dp"
        android:text="@string/txt_cancel_sort"
        android:textColor="@color/black"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>